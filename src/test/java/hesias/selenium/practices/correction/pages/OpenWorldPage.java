package hesias.selenium.practices.correction.pages;

import org.openqa.selenium.WebDriver;

public class OpenWorldPage extends AbstractShopPage {

    public OpenWorldPage(WebDriver driver) {
        super(driver);
    }

}
